<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp8.FlyoutContentPage"
             Title="FlyoutContentPage">
    <VerticalStackLayout>
        <Label
            Text="This sidebar should get visible when screen is wide enough (tablet) for FlyoutPage to be in Split mode, so when Android split view is used on a table the sidebar should be visible when the app takes 2/3 of the screen and in Popover mode when less than 2/3."
            VerticalOptions="Center"
            HorizontalOptions="Center" />
    </VerticalStackLayout>
</ContentPage>